# BudgetBook_Mac

1. Clone Repository
2. Migrate all data & seeding data with "dotnet ef database update"
3. Run in terminal with "dotnet run" (needs .NET SDK)
4. Log in with Demo Account (migrated via Data Seeding Database Update): demo@example.com, Demo123!
